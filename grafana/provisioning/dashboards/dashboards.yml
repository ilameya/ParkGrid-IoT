apiVersion: 1

providers:
  - name: ParkGrid
    orgId: 1
    folder: ParkGrid
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
